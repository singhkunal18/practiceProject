import { Provider, Injectable } from '@angular/core';

export class AppUtils {
    protected static appUtils: AppUtils;
    public apiUrl = '/api';
    public authenticateUrl = '/api';

    constructor() {
    }
    
}
